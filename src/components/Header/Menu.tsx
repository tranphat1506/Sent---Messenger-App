import { BiCategory } from 'react-icons/bi';
import IconButton from '../Buttons/IconButton';

const Menu = () => {
    return <IconButton title="Menu" icon={<BiCategory />} />;
};

export default Menu;
